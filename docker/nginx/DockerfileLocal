FROM --platform=linux/amd64 nginx:1.25.2-alpine

# Copy the public directory
COPY ./public_html /app/

# Copy the nginx config file
COPY ./docker/nginx/nginx.local.conf /etc/nginx/conf.d/default.conf
